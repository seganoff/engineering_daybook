apt packages: build-essential rustc libssl-dev libyaml-dev zlib1g-dev libgmp-dev git libmariadb-dev
dnf install -y rustc libyaml-devel zlib-ng-compat-devel zlib-ng-devel libffi-devel
*mise inst:*
curl https://mise.run | sh
echo 'eval "$(~/.local/bin/mise activate)"' >> ~/.bashrc && source ~/.bashrc

mise use -g ruby@3 (--force) (--verbose)

gem packages: faker mysql2 asciidoctor-pdf asciidoctor-revealjs faraday
(prerequisite: libmariadb-dev (dnf:mysql-devel)  & pull mariadb:lts container)

